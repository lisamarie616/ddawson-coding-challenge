#! npm test ./test/1-double.js

/**
 * Hit 'control + x' to run tests with Atom Script Runner
 */

/**
 * Doubles an integer
 * @param {Number} i The integer to double.
 * @returns {Number} The input doubled
 */
export default function doubleInteger(i) {
  return i * 2; 
}
